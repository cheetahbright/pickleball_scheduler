# pickleball_scheduler

Hosted Streamlit deployment repo for the pickleball scheduler.

- `app.py` is the Streamlit entrypoint used by the hosted app.
- Runtime app code lives under `src/`.
- To enable password protection in the hosted deployment, set the Streamlit
  secret `app_password`.
